// preCollection

(function() {

if (!parsedMeta.type) {
    return;
}

writer.write(newline);
writer.write("// -----------------------------------------------------------------------------" + newline);
writer.write("// - " + collectionName + newline);
writer.write("// -----------------------------------------------------------------------------" + newline);

}());